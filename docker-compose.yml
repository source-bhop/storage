services:
  filebrowser:
    build: .
    container_name: filebrowser
    user: "1000:1000"
    ports:
      - "8040:80"
    volumes:
      - /files:/srv/files
      - filebrowser_cfg:/config
      - filebrowser_db:/database
    restart: unless-stopped

volumes:
  filebrowser_db:
  filebrowser_cfg: